# 2026-07-04 — Repo initialization

> Entry format (use for every entry): **Hypothesis → Setup → Result → Conclusion → Next.** One entry per experiment/investigation. Append-only: supersede, don't rewrite.

## Hypothesis

n/a — bootstrap entry. Establishes the notebook format and records the starting state.

## Setup

Repo scaffolded from `project_brief.md`: workflow config (`docs/agents/`), memory system (ADRs + this notebook + `CONTEXT.md`), 5 difficulty-routed sub-agents (`.claude/agents/`), local issue tracker (`.scratch/`), and Phase-1 seed code (`src/creditaudit/`: schema, calculator, rules) with tests keyed to the brief's Lampiran B worked example.

## Result

- ADR-0001 (modular pipeline) and ADR-0002 (PoT + deterministic calculator) recorded.
- Calculator reproduces Lampiran B: pokok 1.000.000, bunga 0,4%/hari, tenor 30, admin 10% → dana bersih 900.000, total bayar 1.120.000, biaya efektif ≈ 24,4%/bulan.
- Denda computation NOT yet implemented (issue `02-calculator-denda-basis`); lock-cap check currently covers bunga only.

## Conclusion

Foundation matches brief Fase 1–2 targets minus taxonomy finalization (needs human domain judgment) and denda math.

## Next

- `.scratch/fase-1-fondasi/issues/01-finalisasi-taksonomi.md` (hard, human)
- `.scratch/fase-1-fondasi/issues/02-calculator-denda-basis.md` (medium, agent)
- `.scratch/fase-1-fondasi/issues/03-inventaris-sumber-scraping.md` (easy, agent)
